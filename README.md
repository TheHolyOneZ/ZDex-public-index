# ZDex---public-index
A public index of Unreal Engine reflection dumps. Pick a game, pick a build, browse every class, struct, enum and function with real offsets — search it, diff it, download it as .usmap or a C++ SDK.
